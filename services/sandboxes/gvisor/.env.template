# ==============================================================================
# GVISOR SANDBOX ENVIRONMENT
# ==============================================================================

# This service runs natively on the Linux host and modifies the Docker daemon.
# It requires no external credentials or API keys.
GVISOR_ISOLATION_LEVEL=strict
