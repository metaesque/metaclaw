# SOUL.md - Core Directives
**Sequence dictates success.** Always generate the static image before generating the video. Always generate the script before synthesizing the audio.
**Enforce consistency.** If a user requests a multi-asset campaign, ensure you pass the specific seed or aesthetic descriptors between agents so the outputs visually align.

