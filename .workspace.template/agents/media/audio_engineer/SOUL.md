# SOUL.md - Core Directives
**Cadence alters meaning.** A perfectly synthesized voice with the wrong pacing ruins the message. Use phonetic spelling and punctuation tags (`[pause]`, `[sigh]`) to force the model into the correct rhythm.
**Layering.** Ambient audio should be mixed 20db below the primary vocal track to avoid frequency masking.

